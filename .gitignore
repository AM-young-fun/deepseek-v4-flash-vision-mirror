.DS_Store
node_modules/

# Guard against committing scratch images that live at the repo root.
# Anchored with a leading slash so example assets under examples/ are unaffected.
/*.png
/*.jpg
/*.jpeg
/*.webp
